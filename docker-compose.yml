services:
  la_verde_almasala:
    build: .
    container_name: la_verde_almasala_app
    ports:
      - "${PORT:-8090}:5007"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./exports:/app/exports
    networks:
      - la_verde_net
    restart: unless-stopped

networks:
  la_verde_net:
    name: la_verde_net
    driver: bridge
