services:
  verde_clinic:
    build: .
    container_name: verde_clinic_app
    ports:
      - "8089:5007"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./exports:/app/exports
    restart: unless-stopped
