services:
  la_verde_alaboudi:
    build: .
    container_name: la_verde_alaboudi_app
    ports:
      - "${PORT:-8091}:5007"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./exports:/app/exports
    restart: unless-stopped
